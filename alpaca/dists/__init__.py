from os.path import abspath, dirname, join

counts = join(abspath(dirname(__file__)), 'counts.kde')
html = join(abspath(dirname(__file__)), 'html.kde')
objects = join(abspath(dirname(__file__)), 'objects.kde')
